/**
 * Vietnamese API / system error messages.
 * Mirror keys from `../en/errors.ts` and translate values when ready.
 */
export const errorMessages = {
  // Keep empty until VI copy is ready — locale index falls back to English.
} as const
